{
  "name": "crowdshield",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "predev": "kill-port 3000 8000",
    "dev": "concurrently -n frontend,backend -c blue,green \"npm run dev --prefix frontend\" \"backend\\.venv\\Scripts\\python.exe -m uvicorn app.main:app --reload --port 8000 --app-dir backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "kill-port": "^2.0.1"
  }
}
